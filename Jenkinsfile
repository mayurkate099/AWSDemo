
pipeline {
     agent none
     def buildInfo
      stages {
          stage('Gradle build') {
                    buildInfo = rtGradle.run  buildFile: 'build.gradle'
          }
        
         
}
}
